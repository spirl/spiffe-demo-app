{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>spirl/infra//renovate/shared"
  ],
  "kubernetes": {
    "description": [
      "The kubernetes manager has no default file pattern, so images in the raw manifests under k8s/ are invisible to Renovate unless the directory is named here. Without this, spiffe-csi-driver and csi-node-driver-registrar went unupgraded for two years (FARN-225).",
      "This is the one place we depart from spirl/spirl, which configures no kubernetes manager at all and leaves its raw manifests untracked. Those are dev-only fixtures; ours are the demo's documented install path, so they have to stay current. Chart images are not covered here — they live in values.yaml and the built-in helm-values manager reads them."
    ],
    "managerFilePatterns": [
      "/^k8s/.+\\.yaml$/"
    ]
  },
  "packageRules": [
    {
      "description": "This repo's own image. Its version line is driven by hand and sequenced against the chart (see RELEASING.md), so Renovate must not bump it.",
      "matchPackageNames": [
        "/(^|/)spiffe-demo-app$/"
      ],
      "enabled": false
    },
    {
      "description": "Not a real image. k8s/spire/deployment.yaml uses a ko build directive, \"ko://github.com/spirl/spiffe-demo-app\", which the kubernetes manager parses down to a bare \"ko\" package with no version. Nothing can be resolved from it, so disable it rather than let it accumulate lookup failures on the dependency dashboard.",
      "matchManagers": [
        "kubernetes"
      ],
      "matchPackageNames": [
        "ko"
      ],
      "enabled": false
    },
    {
      "description": "Automerge updates that CI actually exercises and that cannot publish an artifact on merge.",
      "matchManagers": [
        "gomod",
        "github-actions"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest",
        "pin",
        "pinDigest"
      ],
      "automerge": true
    },
    {
      "description": "Actions used only by the release workflows. CI never runs them, so a bad bump would surface mid-release. Review by hand.",
      "matchManagers": [
        "github-actions"
      ],
      "matchPackageNames": [
        "helm/chart-releaser-action",
        "ko-build/setup-ko"
      ],
      "automerge": false
    },
    {
      "description": "A push to main touching charts/** publishes the chart via release.yml, and chart-releaser silently no-ops when Chart.yaml version is unchanged. Never automerge here.",
      "matchFileNames": [
        "charts/**"
      ],
      "automerge": false
    }
  ]
}
